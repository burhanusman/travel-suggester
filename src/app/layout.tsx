import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TravelSmart - Discover Uncrowded Destinations",
  description: "Find amazing travel destinations optimized to avoid overcrowding. Plan your perfect trip with real-time crowd density insights and cost-effective recommendations.",
  keywords: ["travel", "destinations", "uncrowded", "vacation", "travel planning", "crowd density"],
  authors: [{ name: "TravelSmart Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
